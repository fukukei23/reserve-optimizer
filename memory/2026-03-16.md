# 2026-03-16 Memory Log

## API Testing Session (00:00-00:10 UTC)

### YouTube Data API Test
- **Status**: Failed - credentials not configured
- **Required files missing**:
  - `config/youtube.credentials.json`
  - `config/youtube.token.json`
- **Action needed**: Google Cloud Console setup + OAuth credentials

### API Tests Conducted
| API | Status | Details |
|-----|--------|---------|
| Brave Search | ✅ OK | 578ms response |
| OpenAI | ✅ OK | Models list retrieved |
| GLM (智谱) | ⚠️ Balance insufficient | Connection OK, but余额不足 |
| Discord Bot | ✅ OK | Message sent successfully |

### ZAI API Endpoint Issue
- **Problem**: Initial test used wrong endpoint `https://open.bigmodel.cn/api/paas/v4`
- **Correct endpoint**: `https://api.z.ai/api/coding/paas/v4` (documented in SHARED.md)
- **Root cause**: Did not check SHARED.md before testing
- **Lesson**: Always check SHARED.md for API endpoints and configuration details

### GLM Available Models (via ZAI)
- glm-4.5
- glm-4.5-air
- glm-4.6
- glm-4.7
- glm-5

---

## Investigation: Why SHARED.md Wasn't Checked

### Root Cause Analysis
1. **AGENTS.md already mandates SHARED.md reading** in "Every Session" section
2. **I failed to follow the rule** - didn't read SHARED.md at session start
3. **memory_search query was insufficient** - "ZAI API endpoint subscription" returned no results
4. **No enforcement mechanism** - rule exists but no automatic check

AGENCY-AGENTS SKILL NOTE:
- `agency-agents` skill exists but was not registered in available_skills (Gateway-managed)

---

## Long-term Fixes Implemented

### 1. Created `skills/agency-agents/SKILL.md` (index)
- Collection index for testing/engineering/specialized sub-skills
- Documents triggers: 'APIテスト', 'テスト', 'benchmark', etc.
- Note: Full registration requires Gateway admin access

### 2. Enforced session-start checks in AGENTS.md
- Mandate: 必ずSHARED.mdを読み、要点を1〜2行に要約してから返信する

---

## Key Actions & Decisions (2026-03-16)  ← NEW

- モデル割り当ての運用ルールを再確認・遵守
  - 軽量タスクは GLM-4.7（早人）を基本にする方針を再確認
  - 誤ってGLM-5を使っていたcronジョブをGLM-4.7へ戻した
    - 対象ジョブ（変更済）:
      - Long Task Watcher
      - weekly_agent_self_diagnosis
      - Weekly OpenClaw Ideas Collector
      - AI YouTube Digest (Daily)
      - daily_obsidian_note

- 日次メモ整理 cron 作成（daily_memory_cleanup）
  - Cron ID: 0d3a02c1-28f8-46d5-8758-7aa73549b39f
  - 実行時刻: 毎日 07:00 JST（22:00 UTC）
  - モデル: zai/glm-4.7
  - 動作: memory/YYYY-MM-DD.md の整理（未解決問題抽出・重複統合・不要ログ削除）
  - 静穏時間ポリシーを尊重（00:00-06:00 JST は緊急のみ通知）

- 静穏時間（Notification Policy）を MEMORY.md に追記
  - 定義: 00:00-06:00 JST（深夜0時〜朝6時）
  - ルール: 静穏時間中は緊急案件のみ通知する

---

## OpenAI 使用量サマリ（集計メモ） ← NEW

目的: OpenAIに切替わった実行のトークン使用量を把握し、各モデル単価で概算コストを出す準備

集計方法と結果（2種類の集計方法を残す）:

1) セッション単位合計（session.totalTokens を集計）
- 対象: sessions のうち modelProvider == 'openai' のエントリを集計
- 合計トークン: **8,975,247 トークン**
- モデル別（session合計）:
  - gpt-5-mini: 5,113,733 トークン
  - gpt-5.1-codex: 3,861,514 トークン
- 備考: sessionレコードに run と session の両方が存在する場合、重複計上の可能性あり（セッション総覧ベースのラフ集計）

2) 実行(run)単位フィルタ集計（より保守的）
- 対象: runレコードや単一実行を意図したエントリのみを集計（cronの親エントリなどの重複を除外）
- 合計トークン: **8,246,711 トークン**（runs = 353）
- モデル別（runフィルタ）:
  - gpt-5-mini: 4,842,288 トークン
  - gpt-5.1-codex: 3,404,423 トークン
- 備考: 実測に近い、重複を減らした集計

注意事項:
- 上記は "合計トークン(入力+出力)" の合算値で、Input/Output の内訳がある場合は別途正確集計可能
- 次ステップ: 指定モデル（例: GPT-5 Mini / GPT-5 / GPT-5 Pro）の単価を当てて概算コスト算出可能（要約算出を実施済みのいずれかの集計方法を選択）

---

## 次にやること（提案・TODO）

1. コスト算出: 上記の runベース（保守的）合計 8,246,711 トークンを基本に代表モデル単価で概算を出す（ユーザー指示待ち）
2. OpenAI側の正確な input/output 内訳が必要ならログから inputTokens/outputTokens を抽出して精密計算する
3. MEMORY.md は長期記憶のため手動での更新を推奨（自動で上書きしない方針）

---

## 参考・出典
- Cloud: 実行はローカル集計（openclaw sessions によるエクスポート）
- 価格参照: OpenAI / 各プロバイダの公開価格表（必要に応じてWeb取得可能）


## 要約（1行）
- 2026-03-16: 軽量タスクはGLM-4.7を基準に戻し、daily_memory_cleanup cron を作成（07:00 JST）、静穏時間を00:00-06:00 JSTに定義。OpenAI使用トークンはセッション集計で約8.98M、実行単位で約8.25M（次はこれを基にコスト算出）。
