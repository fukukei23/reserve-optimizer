# 2026-03-15

## Obsidian日次ノート
- [[obsidian/01_Daily/2026-03-15.md]]

## 要約
チャンネルアーカイブ更新、ファイル整理（USER.md/AGENTS.md/SHARED.md）、Docker全部入りコマンド提示。

## 主なトピック

### 1. チャンネルアーカイブ自動更新
- 2026-03-14の重要事項を各チャンネルアーカイブに追記
- SHARED.mdに「チャンネル・データ保護ルール」追加
- SHARED.mdの合意事項に「Fast Mode有効化」追加

### 2. ファイル整理
- **USER.md**: 興味・関心、副業、コミュニケーションスタイルを追加
- **AGENTS.md**: Heartbeat/Cron詳細をMEMORY.mdに移動して簡潔化
- **MEMORY.md**: Heartbeat/Cron使い分けガイド、コンテナ制約を追加

### 3. Dockerビルドコマンド（全部入り）
- Chromium + inotify-tools + ffmpeg + imagemagick + jq + yt-dlp + pandoc + poppler-utils + zip/unzip
- VPS上で直接実行する必要がある（コンテナ内からは不可）

### 4. コンテナ制約の確認
- コンテナ内からDocker操作はできない
- dockerコマンドなし、権限なし、sudoなし
- DockerビルドはVPS上で実行

## 決定事項
1. 確定申告セクションはSHARED.mdに追加しない（不要）
2. Docker全部入りコマンドを採用
3. チャンネル保護ルール: 削除は人間のみ、ファイル変更30%以上は確認
4. トラブル診断ルール: 原因特定の確認コマンドを先に出す
5. ブラウザ設定: `noSandbox: true` が必須（コンテナ内）

## 完了
- ✅ パッケージ追加（jq, inotify-tools, zip, ffmpeg, yt-dlp, pandoc, poppler-utils）
- ✅ ブラウザ操作有効化（noSandbox: true 追加）
- ✅ ブラウザ使い分けの方針決定（API優先、必要時にSurface Go）
- ✅ 自動マネタイズの方針決定（API優先で設計）

## 次アクション
- なし（ブラウザ操作可能になった）
